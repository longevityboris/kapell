export const meta = {
  name: 'kapell-phase1-foundation',
  description: 'kapell phase 1: CLI core, analysis package, three new musical checks with xray, engines/perform/render/engrave wrappers with path migration, golden tests on The Neighbour; then adversarial review and fixes',
  phases: [
    { title: 'Build', detail: '5 lanes with exclusive files (core, analysis, new checks, engines, golden tests)' },
    { title: 'Review', detail: 'adversarial review of the whole phase, then one fixer' },
  ],
}

const K = '/Users/biobook/Music/llm-music/kapell'
const F = '/Users/biobook/Music/llm-music/fugue-jp/ricercar'
const DOCS = '/Users/biobook/Music/llm-music/fugue-jp/docs/kit'

const COMMON = `
KIT REPO: ${K} (git, branch main; a background job pushes, you only commit). Design you must follow: ${DOCS}/C_packaging_architecture.md (sections 3, 4, 5, 8, 10, 11) and ${DOCS}/PLAN.md. Musical background: ${DOCS}/A_musical_metacritique.md.
STABLE CONTRACTS already in the repo (read them first, extend but never rename): ${K}/src/kapell/commands/__init__.py (command plug-in contract: SPEC, add_arguments, run(args, ctx) -> dict | Result; KapellError; exit codes), ${K}/src/kapell/config.py (lib_dir(), renders_dir()), ${K}/src/kapell/project.py (find_root, load, path), ${K}/src/kapell/analysis/lyparse.py (moved verbatim; parse_voice API unchanged).
ORIGINAL TOOLS: verbatim copies in ${K}/vendor/fugue-jp/ (tools/, design/final-lab/, audio/, orchestration/, performance/, design/proposal-*/). Refactor FROM these into src/kapell; never import from vendor/. Never modify anything under /Users/biobook/Music/llm-music/fugue-jp except where your task says so.
FIXTURE: The Neighbour at ${F} (has kapell.toml; score/music-voices.ly; design/final-lab/). Known facts: check.py on the score gives 0 errors, 0 parallels, 0 beat-parallels, 0 unjustified; suspensions.py gives 18 strong / 13 weak.
Python 3.12 with numpy, scipy, mido, soundfile, music21, pytest. Install the kit for testing with \`uv tool install --force --editable ${K}\` (or run \`python3 -m kapell.cli\` with PYTHONPATH=${K}/src).
RULES: touch ONLY the files your lane owns (listed below); other lanes work in parallel. Commit after every verified step: \`git -C ${K} add <your paths> && git -C ${K} commit -m "<why>" -- <your paths>\` (retry on index.lock). Keep outputs short-by-default JSON (digests), per C section 4. Aim to finish in about 30 minutes; report what is unfinished rather than running long.`

const LANES = [
  { key: 'core', effort: 'high', owns: `src/kapell/cli.py, src/kapell/envelope.py, src/kapell/manifest.py, src/kapell/status.py, src/kapell/__main__.py, src/kapell/commands/{status,doctor,guide,version}.py, tests/unit/test_cli.py, README.md (usage section only)`,
    task: `CLI CORE. Implement kapell/cli.py main(): global --json/--quiet (JSON automatically when stdout is not a TTY), discovery of command modules in kapell.commands (skip modules that fail to import, and report them in \`doctor\`), argparse tree, the envelope and exit codes of C section 3, \`kapell agent-info [--command X]\` built from the SPECs (shape of the manifest excerpt in C section 3), and \`python3 -m kapell\`. Commands: status (C section 3: phase, sections done/open, last check totals, quotas, next command, under 2k tokens; read the project via kapell.project and git log; degrade gracefully when pieces are missing), doctor (lilypond, sfizz_render at config.lib_dir()/bin, sample libraries and IRs present, TYPESAFE_API_KEY present in akm WITHOUT reading its value (\`akm list --json\` names only), Python deps, and command modules that failed to import), guide (prints guides/<name>.md raw; list when no name), version. Unit tests for envelope, exit codes, agent-info schema and discovery.` },
  { key: 'analysis', effort: 'high', owns: `src/kapell/analysis/{__init__,check,harmony,suspensions,strict,grid,spanmap,splice}.py, src/kapell/commands/{check,splice}.py, tests/unit/test_analysis.py`,
    task: `ANALYSIS PACKAGE. Refactor vendor/fugue-jp/tools/check.py and harmony.py, and vendor/fugue-jp/design/final-lab/{suspensions,strict,grid,spanmap,splice_check}.py, into importable modules with no import-time side effects (check.py opens its file at import today), one flag vocabulary (score path, --voices, --bars A-B, --measure, --key accepting both "bes" and range syntax "1-20=bb", --full), and pure functions returning dicts. FIRST (within ~10 minutes) commit stable entry points so the new-checks lane can import them: check.run(path, voices=None, bars=None, measure=None, ranges=None) -> {totals, violations, dissonances}, suspensions.count(path, ...) -> {strong, weak, items}, strict.run(path, ...) -> {clash, acc2, items}, harmony.analyse(path, key=None, bars=None) -> per-attack chords with roman numerals. Behaviour must match the originals on the fixture (0/0/0/0 totals; 18 strong / 13 weak suspensions). Commands: \`kapell check\` (digest by default: totals + violations only, exit 5 on violations) and \`kapell splice --section N|FILE\`. Resolve inputs from the project (kapell.project) when a path is not given. Unit tests on tiny inline fixtures (a parallel fifth, an unprepared fourth, a crossing) plus a fixture check.` },
  { key: 'newchecks', effort: 'high', owns: `src/kapell/analysis/{resolution,coverage,quotas,roles}.py, src/kapell/commands/xray.py, tests/unit/test_new_checks.py, and in the fixture ONLY ${F}/kapell.toml (you may add [themes]/[form]/[features] tables; commit that file in the fugue-jp repo with git -C /Users/biobook/Music/llm-music/fugue-jp)`,
    task: `THE THREE NEW MUSICAL CHECKS + XRAY (C section 5; this needs musical care). (1) resolution.py: chordal sevenths and other tendency tones (leading tone at cadences, flat sixth, raised fourth, suspended dissonances) must resolve by step in the expected direction, in the same voice or a clear hand-off, within N beats; report unresolved ones as DIS7 etc. with bar:beat. It MUST flag the known bar-54 hinge in the fixture (the V4/2 seventh E-flat that goes up to G-flat and leaps), and must not drown the report in false positives (tune thresholds on the whole fixture; report the count). (2) coverage.py: a theme-treatment ledger (occurrences of each declared theme under transposition, inversion, augmentation, diminution, with voice/key/bar) plus a form-claim check: form = "double-fugue" requires S2 to get subject, answer (transposed), inversion or stretto, and a combination with S1; it MUST flag that S2 in the fixture is only heard at one pitch. Declare the fixture's themes in ${F}/kapell.toml (take them from design/final-lab/materials.ly). (3) roles.py + quotas.py: every role the piece model marks as featured (design/final-lab/piece.py roles; add a [features] list to kapell.toml if piece.py lacks a flag) must get a cue in every performance plan under performance/*/; it MUST flag the arioso tenor lament and the bass head imitation; quotas from kapell.toml (strong_suspensions = 20) reported pass/fail using suspensions.count. xray command: one short JSON bundle combining check totals, suspensions, strict, resolution, coverage, roles, quotas (import kapell.analysis.check/suspensions/strict; the analysis lane commits their entry points early, so poll git log for them and wire them in; if one is still missing at the end, call the vendored script by subprocess and say so), exit 5 when any gate fails. Unit tests with tiny fixtures (an unresolved seventh, a theme at one pitch only, a featured role with no cue).` },
  { key: 'engines', effort: 'medium', owns: `src/kapell/engines/**, src/kapell/perform/**, src/kapell/mix/**, src/kapell/piece/engrave.py, src/kapell/commands/{perform,render,engrave,setup}.py, templates/layouts/**, tests/engines/**`,
    task: `ENGINES, PERFORM, RENDER, ENGRAVE (C sections 3 and 8). Move the engine code from vendor/fugue-jp/audio/{piano,strings,organ,orchestra}/ (code, SFZ builders, CONTRACT.md, README, setup scripts, data files the engines need; not QA result dumps) into src/kapell/engines/<name>/, perform.py and the per-version helpers from vendor/fugue-jp/tools/perform.py and vendor/fugue-jp/performance/ into src/kapell/perform/, and orchestrate.py/mix.py into src/kapell/mix/. Replace every hardcoded absolute path (organ_paths.py, setup_organ.sh, piano_paths.py and the rest) with kapell.config.lib_dir()/renders_dir(). Commands: \`kapell perform --version V\` (plan lint can come later), \`kapell render --version organ|piano|quartet|orchestra|ensemble [--bars A-B] [--stems]\` writing to renders_dir(piece)/<version>/ outside git, reading the version specs from the project's performance/<version>/ folder, \`kapell engrave --layout piano|quartet|all\` (the fixture's score/piano.ly and quartet.ly; add organ/orchestra/ensemble layout templates if time allows), \`kapell setup --engine X --check\` (verify only; never download in this phase). Verify end to end on the fixture: render --version piano --bars 1-8 and --version organ --bars 1-8 produce audio whose note count matches the MIDI; do not play audio. Keep the original contracts unchanged.` },
  { key: 'golden', effort: 'medium', owns: `tests/golden/**, tests/conftest.py`,
    task: `GOLDEN TESTS on The Neighbour (C section 10), written FIRST against the ORIGINAL scripts so they are a safety net for the refactor. Capture expected values by running the vendored originals on the fixture: check totals (0/0/0/0 and the full dissonance count), suspensions 18 strong / 13 weak, strict counts, harmony --stats summary, build_sk.py in a temporary copy reproducing SK_final.ly and plan.json byte for byte, lilypond engraving of score/piano.ly and quartet.ly matching the committed PDFs if lilypond output is deterministic (check; otherwise compare page count and a text dump). Then write pytest tests that assert the KIT reproduces them through its API and CLI (kapell.analysis.check.run, suspensions.count, strict.run; \`kapell check --json\`), plus the KNOWN-GAP tests: xray must report the bar-54 unresolved seventh, the S2 coverage failure, suspensions below quota 20, and the missing performance cues for the arioso tenor lament and bass head imitation. Tests for parts other lanes have not landed yet should fail clearly (not be skipped silently). Locate the fixture via env KAPELL_FIXTURE_NEIGHBOUR, default ${F}; skip only if the directory is missing.` },
]

const DONE = {
  type: 'object',
  properties: { done: { type: 'array', items: { type: 'string' } }, unfinished: { type: 'array', items: { type: 'string' } }, tests: { type: 'string' }, notes: { type: 'string' } },
  required: ['done', 'unfinished', 'tests', 'notes'],
}
const REVIEW = {
  type: 'object',
  properties: { defects: { type: 'array', items: { type: 'object', properties: { severity: { type: 'string', enum: ['major', 'minor'] }, where: { type: 'string' }, issue: { type: 'string' }, fix: { type: 'string' } }, required: ['severity', 'where', 'issue', 'fix'] } }, verdict: { type: 'string', enum: ['ready', 'needs-fixes'] } },
  required: ['defects', 'verdict'],
}

phase('Build')
const lanes = await parallel(LANES.map(l => () => agent(`${COMMON}\nYOUR LANE: ${l.key}. You own: ${l.owns}.\n${l.task}\nReturn what is done, what is unfinished, and the test status.`, { label: l.key, phase: 'Build', schema: DONE, effort: l.effort })))
const summary = LANES.map((l, i) => lanes[i] ? `${l.key}: done ${lanes[i].done.length}, unfinished: ${lanes[i].unfinished.join('; ') || 'none'}; tests: ${lanes[i].tests}` : `${l.key}: FAILED`).join('\n')
log(summary)

phase('Review')
const review = await agent(`${COMMON}
ROLE: ADVERSARIAL REVIEWER of kapell phase 1 (effort high). Lane reports:
${summary}
Install the kit (uv tool install --force --editable ${K}), run \`python3 -m pytest ${K}/tests -q\`, then exercise every command on the fixture (cd ${F}; kapell status, doctor, agent-info, check, splice --section 7, xray, engrave --layout piano, render --version piano --bars 1-8). Check against C sections 3, 5, 8 and 10: envelope and exit codes, digests short by default, no import-time side effects, no absolute paths left in src/, the three new checks flag the four known gaps without false-positive floods, golden values reproduced. Do not modify files. Report defects with concrete fixes.`,
  { label: 'review', phase: 'Review', schema: REVIEW, effort: 'high' })
let fix = null
if (review && review.defects.length) {
  fix = await agent(`${COMMON}
ROLE: PHASE-1 FIXER (you may edit any file in ${K}/src, ${K}/tests and ${K}/README.md; lanes are finished). Fix these review findings (major first), make \`python3 -m pytest ${K}/tests -q\` pass, update README.md with install and usage for the phase-1 commands, commit.
${review.defects.map((d, i) => `${i + 1}. [${d.severity}] ${d.where}: ${d.issue}\n   fix: ${d.fix}`).join('\n')}
Return what is done and what is unfinished.`, { label: 'fix', phase: 'Review', schema: DONE, effort: 'medium' })
}
return { lanes: LANES.map((l, i) => ({ lane: l.key, ...(lanes[i] || { failed: true }) })), review: review && { verdict: review.verdict, major: review.defects.filter(d => d.severity === 'major').length, minor: review.defects.filter(d => d.severity === 'minor').length }, fix }
