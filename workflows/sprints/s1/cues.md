You are working in /Users/biobook/Music/llm-music/fugue-wt/s1-cues/ricercar, a git worktree (branch s1-cues) of the piece "The Neighbour" (a 66-bar ricercar a 4 on the Jurassic Park theme). Do NOT commit; leave changes for review. Do NOT edit any score file (score/**) or design/final-lab/** ; you may edit ONLY the performance specs under performance/<version>/ (plan.json, bach_organ.json, quartet.json, symphonic.json and similar spec JSON files; not .py scripts, not audio).

Problem: the kit's check reports featured musical lines with no performance cue in some versions. Run it from this ricercar directory:
  PYTHONPATH=/Users/biobook/Music/llm-music/kapell/src python3 -m kapell xray --full --json
Look at data.roles: "features" (the lines the design marks as featured, e.g. the arioso's tenor lament and the bass head imitation) and "uncued" per version (bach_organ 2, beethoven_piano 3, beethoven_quartet 2, symphonic 1). Read how the checker decides a line is cued (source: /Users/biobook/Music/llm-music/kapell/src/kapell/analysis/roles.py) and how each version's spec expresses cues (roles with role_boost for piano/quartet plans read by perform.py; registration/solo manual for organ; instrument hand-offs and levels for symphonic).
Task: add a musically sensible cue for every uncued featured line in every version, in that version's own idiom: piano/quartet: a "roles" entry (e.g. role "cs" or "subject" or "cf") covering the line's span so it is brought out; organ: put the line on a solo or contrasting registration for its span if the spec supports it, else a role/level cue; symphonic: give the line a clear solo colour or level for its span. Keep all other spec content unchanged. Validate every JSON file you touch.
Done when xray reports roles ok (no uncued lines) and the JSON files parse. Finish with a short report listing each cue you added (version, voice, bars, how).
