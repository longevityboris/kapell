You are working in /Users/biobook/Music/llm-music/kapell-wt/s1-jev, a git worktree (branch s1-jev) of kapell, a Python CLI composition kit. Do NOT commit; leave changes for review. Create or edit ONLY: src/kapell/jev/** (new package), src/kapell/commands/jev.py, tests/jev/**.

Goal: the kit's Jev module, used only for orchestration glue (not musical judgement). Jev is TypeSafe AI's decision model (API docs: https://docs.typesafe.ai/llms.txt; primitives Choice, Score, Noul; confidence). A working reference client and the measured calibration are in /Users/biobook/Music/llm-music/fugue-jp/docs/kit/jev_probe.py, /Users/biobook/Music/llm-music/fugue-jp/docs/kit/D_jev_probe.md and /Users/biobook/Music/llm-music/fugue-jp/docs/kit/jev_probe_summary.json; read them first and reuse the request format.
Build:
1. src/kapell/jev/client.py: minimal client (stdlib urllib or requests if installed), key from env TYPESAFE_API_KEY (the kit is run as `akm run TYPESAFE_API_KEY -- kapell jev ...`; never print or log the key; if missing raise KapellError(code "jev_key_missing", exit_code 2) from kapell.commands). Retries on 429/5xx with backoff (exit 4 when rate limited after retries). Pin the model id in one constant.
2. src/kapell/jev/questions.py: two gated decisions measured to work (D_jev_probe.md): route(finding) -> "notes" | "performance" with confidence; park(finding) -> "minor" | "major" where only "minor" at high confidence is trusted (everything else escalates). Thresholds as constants with the probe's measured precision in comments.
3. src/kapell/commands/jev.py following the plug-in contract in src/kapell/commands/__init__.py: `kapell jev route FINDINGS.json` and `kapell jev park FINDINGS.json` (input: a list of review findings {severity, where, issue, fix}; output: per finding the decision, confidence, and "escalate": true/false; short digest by default).
4. tests/jev/: unit tests with the HTTP layer mocked (no network, no key needed), plus one live test skipped unless TYPESAFE_API_KEY is set.
Run: PYTHONPATH=src python3 -m pytest tests/jev -q. Finish with a short report.
